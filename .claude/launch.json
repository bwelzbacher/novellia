{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "frontend",
      "cwd": "${workspaceFolder}/frontend",
      "runtimeExecutable": "/bin/bash",
      "runtimeArgs": [
        "-c",
        "if [ -s \"$HOME/.nvm/nvm.sh\" ]; then . \"$HOME/.nvm/nvm.sh\"; nvm use >/dev/null 2>&1 || true; fi; exec node node_modules/@angular/cli/bin/ng.js serve --port \"$PORT\""
      ],
      "port": 4200,
      "autoPort": true
    }
  ]
}
